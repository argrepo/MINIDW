package com.anvizent.packagerunner.service;

import java.util.Map;

import com.anvizent.packagerunner.model.SchedulerInfo;



public interface PackageService {
	
	boolean runPackages(SchedulerInfo schd);

	Map<Long, SchedulerInfo> getPackagesList();
	
	SchedulerInfo getScheduleInfo(long id);

	int getMapSize();
	
	SchedulerInfo getScheduleRemoveId(long id);
}
